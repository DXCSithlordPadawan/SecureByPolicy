{
  "standard": "C++ Security Best Practices Guide",
  "version": "1.0",
  "compliance_baseline": ["OWASP-Top10", "NIST-SP-800-53", "DISA-STIG", "FIPS-140-3", "CIS-L2"],
  "file_extensions": [".cpp", ".cc", ".cxx", ".hpp", ".hh"],
  "forbidden_patterns": [
    {
      "pattern": "\\bgets\\s*\\(",
      "reason": "DISA STIG / CWE-120: gets() performs no bounds checking and always causes a buffer overflow vulnerability.",
      "severity": "Critical",
      "remediation": "Replace gets() with std::getline(std::cin, str) for C++ code, or fgets(buf, sizeof(buf), stdin) for C-style I/O."
    },
    {
      "pattern": "\\bstrcpy\\s*\\(",
      "reason": "DISA STIG / CWE-120: strcpy() performs no bounds checking and can overflow the destination buffer.",
      "severity": "High",
      "remediation": "Replace with std::string assignment, or use strlcpy(dst, src, sizeof(dst)) when C-style char arrays are required."
    },
    {
      "pattern": "\\bstrcat\\s*\\(",
      "reason": "DISA STIG / CWE-120: strcat() performs no bounds checking and can overflow the destination buffer.",
      "severity": "High",
      "remediation": "Replace with std::string concatenation (+=) or strlcat(dst, src, sizeof(dst)) for C-style buffers."
    },
    {
      "pattern": "\\bsprintf\\s*\\(",
      "reason": "DISA STIG / CWE-120: sprintf() has no output length limit and can overflow the destination buffer.",
      "severity": "High",
      "remediation": "Replace with snprintf(buf, sizeof(buf), ...) or std::ostringstream for safe string formatting."
    },
    {
      "pattern": "\\bsystem\\s*\\(",
      "reason": "OWASP A03 / DISA STIG: system() invokes a shell and is vulnerable to command injection.",
      "severity": "Critical",
      "remediation": "Replace system() with execve() using an explicit argument array, or use a platform-safe alternative like boost::process."
    },
    {
      "pattern": "\\bpopen\\s*\\(",
      "reason": "OWASP A03 / DISA STIG: popen() invokes a shell and is vulnerable to command injection.",
      "severity": "Critical",
      "remediation": "Use pipe()/fork()/execve() directly, or a safe process library, to avoid shell interpretation."
    },
    {
      "pattern": "\\brand\\s*\\(\\s*\\)|\\bsrand\\s*\\(",
      "reason": "FIPS 140-3 / NIST SP 800-90A: rand() and srand() are not cryptographically secure.",
      "severity": "High",
      "remediation": "Use std::random_device with std::mt19937 for general randomness, or a FIPS-validated DRBG (e.g., OpenSSL RAND_bytes()) for security operations."
    },
    {
      "pattern": "new\\s+\\w[^;]*;(?!.*if.*==.*nullptr|.*assert|.*check)",
      "reason": "CWE-252 / OWASP A05: Raw new without nullptr check can result in uncaught bad_alloc or null pointer dereference.",
      "severity": "Medium",
      "remediation": "Use smart pointers (std::unique_ptr, std::shared_ptr) instead of raw new to ensure automatic memory management and exception safety."
    },
    {
      "pattern": "reinterpret_cast\\s*<",
      "reason": "CWE-704 / OWASP A05: reinterpret_cast bypasses the type system and can enable undefined behavior.",
      "severity": "Medium",
      "remediation": "Prefer static_cast or dynamic_cast. Use reinterpret_cast only with careful documentation of the safety invariants."
    },
    {
      "pattern": "MD5_Init|MD5_Update|MD5_Final|EVP_md5\\s*\\(",
      "reason": "FIPS 140-3 / STIG V-222645: MD5 is cryptographically broken and not FIPS-compliant.",
      "severity": "High",
      "remediation": "Replace with SHA-256 (EVP_sha256()) via the OpenSSL EVP interface."
    },
    {
      "pattern": "SHA1_Init|SHA1_Update|SHA1_Final|EVP_sha1\\s*\\(",
      "reason": "FIPS 140-3 / NIST SP 800-131A: SHA-1 is deprecated and not FIPS 140-3 compliant.",
      "severity": "High",
      "remediation": "Replace with SHA-256 (EVP_sha256()) or stronger."
    },
    {
      "pattern": "password\\s*=\\s*['\"][^'\"]{4,}['\"]",
      "reason": "OWASP A07 / NIST IA-5: Hardcoded password detected in C++ source.",
      "severity": "High",
      "remediation": "Remove hardcoded passwords. Read credentials from secure environment variables or an encrypted configuration file."
    }
  ]
}
