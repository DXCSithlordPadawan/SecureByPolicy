# Python dependencies for the SecureByPolicy Modular Security Gatekeeper
# All packages must be verified against the internal approved software list
# before use in a FIPS 140-3 / STIG-compliant environment.

# Bandit — server-side static analysis (DISA STIG V-222637)
bandit==1.7.9

# Example: Ruff for fast Python linting (install separately on dev workstations)
# ruff==0.4.7
